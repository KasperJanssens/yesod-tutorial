$(function() {
    $("##{rawJS voegToe}").click(function(event){
        console.log("preventing default");
        event.preventDefault();
        var liElementText = event.target.innerText;
        var newNode = $("<li></li>");
        var inputText = "<input id=42 type=text name=f1 checked>";
        newNode.append(inputText);
        $("##{rawJS ulId}").append(newNode);
        return false;
    });
});